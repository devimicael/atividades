// TODO: 1 -  Verifique se um número é positivo, negativo ou zero usando declarações if, else if e else.

const numero = 20;

const result = numero === 0 ? "número é 0": numero < 0 ? "número é negativo": "número é positivo";
console.log(result); 